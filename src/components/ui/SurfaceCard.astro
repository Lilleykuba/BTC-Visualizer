---
import { cn } from "@/lib/utils/cn";

interface Props {
  class?: string;
  padded?: boolean;
}

const { class: className, padded = true } = Astro.props;
---

<section class={cn("panel overflow-hidden", padded && "p-6 md:p-8", className)}>
  <slot />
</section>
